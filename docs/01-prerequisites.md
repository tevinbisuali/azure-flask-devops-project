
---

# Prerequisites

Before starting, install the following tools:

- Git
- Terraform
- Azure CLI
- Visual Studio Code
- OpenSSH

Verify installations:

git --version
terraform --version
az --version
ssh -V
